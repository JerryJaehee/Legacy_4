<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.iu.s1.member.MemberDAO">
	<insert id="join" parameterType="MemberDTO">
		INSERT INTO MEMBER VALUES(#{id}, #{pw}, #{name}, #{phone}, #{email})
	</insert>
	<select id="login" resultType="MemberDTO" parameterType="MemberDTO">
		SELECT ID,NAME FROM MEMBER WHERE ID=#{id} AND PW=#{pw}
	</select>
	<select id="mypage" resultType="MemberDTO" parameterType="MemberDTO">
		SELECT ID,NAME,PHONE,EMAIL FROM MEMBER WHERE ID=#{id}
	</select>
</mapper>